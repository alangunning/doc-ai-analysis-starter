name: Validate Markdown
description: Compare original documents with their Markdown rendering.
model: openai/gpt-4o-mini
modelParameters:
  temperature: 0
messages:
  - role: system
    content: |-
      You verify that a Markdown rendering matches the original document.
  - role: user
    content: |-
      Compare the provided PDF and Markdown content. Respond with JSON {"match": bool, "reason": string?}.
